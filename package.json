{
  "name": "search-cli",
  "version": "1.0.0",
  "description": "A simple search command line interface, built with Node.js",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Caledonia Thomson",
  "license": "ISC",
  "bin": {
    "search-cli": "./cli.js"
  },
  "devDependencies": {
    "ramda": "^0.25.0"
  },
  "dependencies": {
    "perf_hooks": "0.0.1"
  }
}
