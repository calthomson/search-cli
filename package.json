{
  "name": "search-cli",
  "version": "1.0.0",
  "description": "A simple search command line interface, built with Node.js",
  "main": "index.js",
  "scripts": {
    "test": "npm run build && ./node_modules/mocha/bin/mocha build/test",
    "build": "babel --presets es2015 src --out-dir build",
    "start": "npm run build && node build/cli.js"
  },
  "author": "Caledonia Thomson",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "chai": "^4.1.2",
    "mocha": "^5.2.0",
    "ramda": "^0.25.0"
  },
  "dependencies": {
    "colors": "^1.3.2",
    "inquirer": "^6.2.0",
    "performance-now": "^2.1.0"
  }
}
